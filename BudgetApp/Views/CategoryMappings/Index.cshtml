@model IEnumerable<BudgetApp.Models.CategoryMapping>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Keyword)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryNameNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Keyword)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryNameNavigation.CategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Cmid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Cmid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Cmid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
